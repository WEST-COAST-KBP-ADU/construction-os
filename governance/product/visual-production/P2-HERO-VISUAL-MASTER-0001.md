# P2 Hero Visual Master 0001

Status: **REVIEW EVIDENCE — PRODUCTION USE REFUSED**

Issue #331 joins the Owner-selected `R1 + R2 + R3` direction into one coherent,
rights-clean visual system: a concept-only detached A600 is immediately legible
on a representative Greater Sacramento residential lot; authored camera,
landscape and warm material discipline support it; a causal planning/project
graph resolves toward a secondary KBP Construction OS plane only after physical
product truth is established.

The visual master is generated by
`tools/visual/p2_hero_visual_master.py` and the merged camera/motion generator
`tools/visual/p2_hero_scene.py`. The render-source commit is
`82eeb9c0dc1337eda19a60fa1033bf07bc4a372e`. It contains the byte-exact
generators and six returned artifacts whose digests are recorded in
`p2-hero-visual-master-manifest-v1.json`.

## Visual and physical boundary

- A600 geometry derives only from the three `OWNED` ledger records. It keeps the
  20 × 30 ft envelope, 9 ft plate, 4:12 gable and concept-only limits.
- Lot, existing-house opening, hardscape, fence, landscape, planning marks and
  OS plane are packet-authored Blender primitives in a synthetic,
  non-identifiable representative environment.
- Materials are constant procedural Blender node graphs in limestone, warm
  bone, charcoal, restrained clay/copper and sage. Lighting is procedural.
- No external mesh, texture, HDRI, stock media, photography, Adobe composite,
  ImageGen, manufacturer media, external font, purchase or unknown-rights
  constituent is present.
- The Blender built-in `Bfont` data-block carries the small internal OS label;
  it references no external font file and reopens with zero missing paths.

Conceptual A600 visualization on a representative property — not a completed
project, parcel-specific fit, approved plan, permit, price, schedule, or
buildability conclusion.

## Render and join evidence

The adopted studio-stack validator passed while truthfully retaining
`PRODUCTION_USE: REFUSED`. A fresh Lambda doctor returned `DOCTOR_PASS` and
proved Blender 5.2.0 LTS with NVIDIA A10 (23,028 MiB). The final Cycles render
bound `CUDA:NVIDIA A10`, set the scene device to `GPU`, refused CPU fallback,
and was observed by the NVIDIA driver 115 times.

The returned `.blend` was reopened in Blender 5.2.0 LTS. It reported zero
linked libraries, zero external images, zero image-texture nodes, zero missing
paths, `GPU`, the exact conceptual disclosure and `PRODUCTION_USE: REFUSED`.
The responsive PNGs were visually inspected as one contact sheet: each retains
an existing-house opening cue, yard depth, the A600 roof and base, and the
causal path. The MP4 is H.264, 960 × 600, 24 fps and 26 frames.

All J01–J10 checks pass at the render-source commit against the declared
generators, reopened `.blend`, constituents and recomputed SHA-256 digests.
This is review evidence for an Owner visual gate; it is not approval, merge,
deployment, Production use, a completed-project claim or a public claim.
